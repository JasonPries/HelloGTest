language: cpp

addons:
  apt:
    packages:
      - cmake
    sources:
      - kalakris-cmake

compiler:
  - clang
  - gcc

before_install:
  - pip install --user cpp-coveralls

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - ./run_tests
